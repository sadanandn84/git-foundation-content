# Unstage/Restore and Unmodify files

git restore --staged <filename>
git restore --staged .

# By mistake someone deleted or modified the code file, and now I want to get the content of previous version, I will use:
git checkout -- <filename>

PS: The above command will discard al your current changes in the working directory

git checkout -f  # It will rollback to previous commit.
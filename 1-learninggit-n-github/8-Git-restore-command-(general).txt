# Git restore command (general) and Git Restore command (from staging area)

# If file is staging area, use below command
git restore --staged <filename>

# once the file is restored from staging, then use
git restore . or filename


# git restore doesn't works for Untracked files. So, delete the untracked files manually.